# Instruções

## SITE
> Fica na branch `site`

hugo
cd public
git checkout site
git add .
git commit -m "My new site"
git push origin site --force

## Código fonte
> Fica na branch `codigo-fonte`
cd ..
git checkout codigo-fonte
git add .
git commit -m "My new site"
git push origin codigo-fonte --force

